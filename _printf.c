#include "main.h"

/**
 * _printf - placeholder implementation.
 * @format: format string (unused in stub)
 *
 * Return: 0 always (no output). Real implementation will be added.
 */
int _printf(const char *format __attribute__((unused)), ...)
{
    return (0);
}
